/*
 * Given an array S of integers, are there elements a, b, c in such that a + b + c = 0? Find all unique triplets in the array which gives the sum of zero.
 * Time : 
 * Space:
 */

#include <iostream>

using namespace std;

class Solution
{
public:
	vector<vector<int>> Three_sum(vector<int>& nums)
	{
		vector<vector<int>> ans;
	}
};

int main()
{
	
	
	return 0;
}
